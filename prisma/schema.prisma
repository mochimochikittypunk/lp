generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("POSTGRES_URL")
}

model Section {
  id      String @id @default(cuid())
  key     String @unique // e.g., "hero_title", "vision_text", "contact_email"
  content String
}

model Project {
  id           String  @id @default(cuid())
  title        String
  desc         String
  url          String
  thumbnail    String?
  displayOrder Int     @default(0)
}
